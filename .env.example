# Copy thanh .env (tuy chon): copy .env.example .env
# docker compose tu doc file .env

APP_PORT=8080
DB_USER=hydro_reader
DB_PASSWORD=hydro_reader

# Production: openssl rand -hex 32
JWT_SECRET=dev-only-change-me-use-openssl-rand-hex-32
JWT_EXPIRATION_SECONDS=86400
