# Profile cho Render.com (free): PORT + Postgres tu bien DB_*
spring:
  config:
    activate:
      on-profile: render
  datasource:
    url: jdbc:postgresql://${DB_HOST}:${DB_PORT}/${DB_DATABASE}
    username: ${DB_USER}
    password: ${DB_PASSWORD}

server:
  port: ${PORT:8082}
