# Chế độ thử: chỉ HTTP, không HTTPS — mở http://IP_VPS hoặc http://localhost
:80 {
	root * /srv
	encode gzip

	handle /api/* {
		reverse_proxy backend:8082
	}

	handle {
		try_files {path} /index.html
		file_server
	}
}
